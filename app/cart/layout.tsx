export const metadata = {
  title: 'Shopping cart - Review your order before pickup | Digital Bazar',
  description: 'Review your cart from local shops in Nagpur. One shop per order, real inventory reserved transactionally, pickup with QR verification.',
  alternates: { canonical: '/cart' },
};
export default function CartLayout({ children }: { children: React.ReactNode }) {
  return children;
}
